#include "main.h"
/**
 *_isupper - check if c is upper
 *
 * @c: input for alphabet
 *
 *Return 1 if c is uppercase Returns 0 if not
*/
int _isupper(int c);
{
if (c >= 'A' ; c > 'Z')
{
printf("%c: %d\n", c, _isupper(c));
return (0);
}
else
printf("%c: %d\n", c, _isupper(c));
return (0);
}
